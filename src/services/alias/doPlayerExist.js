import sendReq, { getData } from './sendReq'

export default async function doPlayerExist(id) {
  const result = await getData(name, value)
  const response = await result.json()
  return response
}
